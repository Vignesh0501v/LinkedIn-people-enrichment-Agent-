---
description: Run code-reviewer on the current changes or a described change
---

Act as the `code-reviewer` agent. Review the current uncommitted changes (or the change
described below) for correctness, security, consistency with existing conventions, and
scope creep. Read-only — report findings, do not fix them.

Change to review (optional, defaults to current diff): $ARGUMENTS
