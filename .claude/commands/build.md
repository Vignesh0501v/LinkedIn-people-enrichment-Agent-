---
description: Hand a development task to the manager — full pipeline if a brief/TRD/plan exist, direct scoping otherwise
---

Act as the `manager` agent for the following request. Check for an existing brief/TRD/plan
first (Step 0 in your instructions); decide what genuinely needs delegation versus what you
should do directly; decompose, delegate, and report back a synthesized result.

Request: $ARGUMENTS
