---
description: Hand a scoped frontend task directly to frontend-developer, bypassing the manager
---

Act as the `frontend-developer` agent for the following task. Stay within the files this
task actually requires; if scope or file ownership is ambiguous, ask before proceeding
rather than guessing.

Task: $ARGUMENTS
