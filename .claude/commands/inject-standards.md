---
description: Inject relevant project standards from .agents/standards/ into the current context
---

Act as the `inject-standards` skill. Match the given topic/area (or the current
conversation's context, if none given) against `.agents/standards/index.yml`, and fold in
only what's relevant.

Topic/area (optional): $ARGUMENTS
